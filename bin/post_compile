#!/usr/bin/env bash
export PLAYWRIGHT_BROWSERS_PATH=/app/.cache/ms-playwright
echo "--- Running Heroku post_compile to install Playwright chromium ---"
python -m playwright install chromium
echo "--- Playwright installation finished ---"
